# cases/resume_test.case.yml
cases:
  - name: test_resume_001_success
    description: "This case should pass."
    tool: demo-tool
    params:
      item: "case 1"

  - name: test_resume_002_failure
    description: "This case should fail."
    tool: non-existent-tool # This tool does not exist, causing a failure
    params:
      item: "case 2"

  - name: test_resume_003_success
    description: "This case should be skipped on the first run."
    tool: demo-tool
    params:
      item: "case 3"
